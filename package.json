{
  "name": "newtown-surveying",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.input.css -o ./styles.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2"
  }
}
